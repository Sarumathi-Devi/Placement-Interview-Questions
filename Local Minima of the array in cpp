    int bfr = 0;
    int nxt = 2;
    
    if ( a[0] <= a[1] ) cout << a[0] << " ";
    
    for ( i = 1; i < n-1; i++ )
        if ( a[bfr++] >= a[i] && a[i] <= a[nxt++] )
            cout << a[i] << " ";
    
    if ( a[n-1] <= a[n-2] ) cout << a[n-1];
